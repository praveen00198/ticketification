# Frontend Environment Variables
# For local development with Vite proxy (defaults to http://localhost:5000 via proxy if left empty):
# VITE_API_URL=

# For production deployment on Vercel (points to Render backend):
VITE_API_URL=https://ticketification.onrender.com
